#ifndef CLI_COMMANDS_H
#define CLI_COMMANDS_H

/**
 * @brief Initialize custom CLI commands (json, info, data)
 * Call this from emberAfMainInitCallback()
 */
void customCliInit(void);

#endif // CLI_COMMANDS_H
